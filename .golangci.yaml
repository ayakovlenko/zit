---
linters:
  disable-all: false
  enable-all: true
  disable:
    - exportloopref # deprecated
    - gomnd # deprecated
    - execinquery # deprecated
    - depguard

linters-settings:
  varnamelen:
    ignore-names:
      - fs
